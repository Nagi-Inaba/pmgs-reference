# Python 3.13 hosted verification — 2026-08-22

対象PR: #39

## 目的

package metadataとcontributor向け文書がサポート対象としているPython 3.13を、source checkoutとinstalled wheelの両方でGitHub-hosted runner上から実測する。

## Hosted CI evidence

commit `833889e23b3dd7f44ebd2348229c7a9f9c74cc22`をGitHub Actions CI run `32566343602`（run #252）で検証した。

### Python 3.13 source job

- runner: Ubuntu latest
- interpreter: CPython 3.13.12
- repository boundary: 179 candidate files、違反なし
- Ruff check: success
- Ruff format check: `97 files already formatted`
- mypy: `Success: no issues found in 29 source files`
- pytest: `247 passed, 5 skipped in 29.02s`
- wheel / sdist build: success

skip 5件はすべてWindows固有の既存契約である。

- Windows no-replace rename contract
- Windows command lookup behavior 2件
- Windows cmd.exe integration test
- Windows junction coverage

Python 3.13固有のfailureまたはskipは0件だった。

### Installed wheel

`Installed wheel on Python 3.13` jobが成功し、既存のisolated-wheel E2Eを通じてbuild済みwheelからsetup、再setup、doctor、lookup、stdio MCP、versionを検証した。

### 既存matrix

Python 3.12 / 3.14のUbuntu、Windows、macOS、installed wheel 3 OS、Cloudflare Worker、synthetic determinism 3 OSとcross-OS compareも同じrunで成功した。

## 契約

Python 3.13はUbuntuのfull source validationとinstalled-wheel E2Eを必須の追加coverageとする。OS依存の回帰検証は引き続きPython 3.12 / 3.14の3 OS matrixで行う。workflowを追加しただけでは`observed`とせず、hosted run IDと測定結果を本記録のように保存する。

この記録追加後、最新`main`を取り込んだ最終headでrequired CIを再実行し、マージ判定に使用する。
